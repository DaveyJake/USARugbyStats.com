<?php
$form->prepare();
$form->setAttribute('action', $this->url('zfcadmin/usarugbystats_competitionadmin/edit', ['id' => $entity->getId()]));
$form->setAttribute('method', 'post');
?>

<h1>Update Competition</h1>

<?php echo $this->render('usa-rugby-stats/competition-admin/competition-admin/form', $this->vars()); ?>
